__pycache__/
*.pyc
.env
chroma_db/
.venv/
venv/
.pytest_cache/
.ruff_cache/

# 검증용 임시 스크립트 (배포 제외)
_smoke.py
_diag.py
_e2e.py
_e2e.ps1

# Claude Code MCP 등록 (PC 절대경로가 박혀 있어 공개 제외 — .mcp.json.example로 공유)
.mcp.json

# 내부 작업 규칙(dogfooding 설명) — 로컬 유지, 공개 제외
/CLAUDE.md

# 내부 진행 기록 — 로컬 유지, 공개 제외
/progress.md

# superpowers 작업 산출물(플랜·스펙) — 공개 제외
/docs/superpowers/

# 엔진 자동 기동 로그 (런타임 산출물)
engine.log
